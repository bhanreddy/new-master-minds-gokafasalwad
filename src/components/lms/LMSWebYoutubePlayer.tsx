import { View } from 'react-native';

/** Native/iOS/Android: modal uses `react-native-youtube-iframe` instead; this file is unused. */
export default function LMSWebYoutubePlayer() {
  return <View />;
}
